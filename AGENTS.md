# soop-chat 에이전트 작업 맥락

최종 검토: 2026-08-22 — 플레이어 빌드 `202603240400`과 합계 약 103시간 17분, 928,414개 패킷의 실방송 캡처를 대조했다. 공식 `LivePlayer.js`의 69개 실제 switch 분기를 전수 대조해, 플레이어가 필드를 읽지만 raw로 남아 있던 연결 응답·귓속말·후원·슬로우모드·상품·VR·모바일 송출 일시정지·강퇴 관리·아이템 이벤트 22개를 추가로 구조화했다. 기존 이벤트의 누락 필드와 `stationAdcon.chatNo` 타입, 대결미션·Nightbot 상세값도 공식 분기에 맞췄다. 투표 상태, 방송 중 19금 전환, 방송 대기 없는 즉시 종료, 구독 선물권 지급·사용, VOD 구독 출처, 퀵뷰 플러스 선물, 후원 팬클럽 가입, 영상풍선과 운영자 공지를 실방송 화면과 대조했다. `LiveView.js`의 구독 상품표 57개 행과 퀵뷰 상품 분기는 이벤트 문맥에 맞춰 연결한다. 공개 카탈로그는 known opcode 101개와 `unknown`으로 구성되며, 전체 이벤트 색인과 구조화된 62개 opcode의 공개 필드는 `docs/events.md`에 정리한다. `0088 closeBroad` 수신 시 종료 이벤트를 전달하고 재연결하지 않는다. TypeScript 7과 최신 `ws`, Playwright를 사용하되 최소 지원 런타임에 맞춰 `@types/node`는 Node 24 계열을 유지한다. 타입 인식 Oxlint와 Oxfmt를 기본 검사로 사용하고 `.gitattributes`에서 LF 줄바꿈을 고정한다. 단위 테스트 16개와 Chromium/Firefox/WebKit 스모크 테스트, `npm pack` 검사를 유지한다. npm 배포를 위해 비공식 읽기 전용 라이브러리임을 명시한 설명, 저장소·이슈·작성자·검색 메타데이터를 제공하고 `docs`를 패키지에 포함한다. 장시간 수집용 동반 CLI는 `../soop-chat-test`에 있으며 raw 패킷과 라이브러리의 분석 이벤트를 함께 저장한다. 수집 DB의 스키마 마이그레이션은 지원하지 않는다.

## 작업 규칙

- 사용자가 명시적으로 코드 수정을 요청하기 전에는 코드를 수정하지 않는다.
- 사용자가 명시적으로 커밋을 요청하기 전에는 Git 커밋을 만들지 않는다.
- 코드를 변경할 때마다 이 파일도 같은 변경에서 갱신한다. 시간순 변경 로그를 누적하지 말고, 위의 현재 상태와 아래에서 영향을 받은 맥락을 최신으로 유지한다.
- 코드 변경 후 `npm run format`을 적용하고 `npm run typecheck`, `npm run lint`, `npm run format:check`, `npm run test:unit`을 통과시킨다.
- 커밋 메시지는 기존 이력처럼 Conventional Commits의 영문 type(`feat`, `fix`, `build`, `chore` 등) 뒤에 한국어 요약을 쓴다.

## 목적과 현재 범위

- `soop-chat`은 공개 SOOP 라이브 채팅을 익명으로 읽는 ESM 전용 TypeScript npm 라이브러리다.
- Node.js 24 이상에서는 SOOP 라이브 정보 API를 통해 방송인 아이디를 해석하며, 기본 진입점에서 같은 조회 로직인 `resolveNodeChannel`을 서버용 공개 API로 제공한다. 브라우저는 `soop-chat/browser`를 사용하며, SOOP 라이브 정보 응답의 CORS 허용 Origin이 플레이어로 제한되어 있으므로 애플리케이션 서버가 반환한 `ChannelInfo`를 전달하는 `ChannelResolver`를 주입해야 한다.
- v0.1.0은 이벤트 읽기만 지원한다. 로그인, 비밀번호 방, 구독플러스 방, 19금 방, 채팅 전송은 불완전한 API 형태로 미리 노출하지 않는다.
- 공개 이벤트 유니온은 현재 확인된 네트워크 opcode 101개와 `unknown` 변형을 합친 총 102개의 손실 없는 변형으로 구성된다.
- `README.md`는 설치와 사용법을 안내하고, `docs/events.md`는 공개 이벤트 API를, `docs/protocol.md`는 wire protocol 분석과 관찰 근거를 설명한다.

## 프로토콜 핵심 사실

- WebSocket URL은 `wss://<소문자 CHDOMAIN>:<CHPT + 1>/Websocket/<streamerId>`이며 서브프로토콜은 `chat`이다.
- 프레임 헤더는 14바이트다. `ESC TAB`, ASCII opcode 숫자 4자리, ASCII payload 바이트 길이 6자리, flag 숫자 2자리 뒤에 UTF-8 payload가 온다.
- payload 필드는 form feed(`0x0c`)로 구분한다. 길이는 JavaScript 문자열 길이가 아니라 UTF-8 바이트 수다.
- 익명 연결 순서는 `0001` connect(`\f\f\f16\f`), 서버의 `0001` 응답 대기, `0002` join(`\f<CHATNO>\f\f\f\f\f`), 이후 60초마다 `0000` keepalive 전송이다.
- 실방송 종료 직전에 `0007 status=0`, 빈 payload의 `0088`, `0007 status=0` 순서가 관찰됐다. 방송 대기 없이 종료된 새 캡처에서는 `0007 status=0` 1ms 뒤 `0088`이 왔고 3ms 뒤 소켓이 닫혔다. 플레이어 enum의 `0088` 이름은 `SVC_CLOSE_BROAD`다. 라이브러리는 `0088`을 typed 이벤트로 먼저 노출한 뒤 `ended: offline`을 한 번 전달하고 소켓을 닫으며 재연결하지 않는다.
- `0007 SVC_SETBJSTAT`은 방송인 상태 원본 값이다. 새 캡처의 263건 중 마지막 종료 시점의 1건을 제외한 262건이 방송인 계정의 `0004` 입장·퇴장과 거의 같은 시각에 발생했고 실제 방송 중에도 `0`과 `1`이 반복됐다. 영상 송출·방송 대기·종료 상태로 해석하지 않고 `status` 숫자만 제공한다.
- 방송 대기 진입에는 별도 채팅 opcode가 관찰되지 않았다. 새 캡처에서는 `17:18:13`경 대기 화면으로 전환된 뒤 채팅 소켓이 약 10분 유지되고 `17:28:11.047`의 `0088`에서 실제 종료됐다.
- 방송 중 19금 전환 시각에는 전용 채팅 opcode나 연결 상태 변화가 없었고, 전환 전에 연결된 익명 채팅 WebSocket은 종료까지 유지됐다. 같은 10초 구간의 정상 퇴장이 88건으로 증가하고 이후 입장이 급증했지만 이는 간접 상관관계일 뿐 성인 제한 신호로 해석하지 않는다. 채널 리졸버는 최초 연결과 재연결 때만 호출되므로 연결 중 제한 변경을 이벤트로 만들지 않는다.
- `0004`의 퇴장은 공식 플레이어처럼 `quitFlag === 1`이면 일반 퇴장, 그 밖에는 강퇴로 본다. 새 캡처의 강퇴 6건은 모두 `quitFlag=2`였고 `etcInfo`는 1 또는 2였으나 의미를 확인하지 못했다. 기존 필드를 보존하면서 `isKicked`를 제공한다.
- `0008`은 채팅금지 대상, 초 단위 지속 시간, 누적 횟수, 명령 주체 ID·타입·표시값을 구조화한다. 화면 기억·별도 확인 경로 및 공식 플레이어와 대조해 `commanderType=1`은 방송인, `2`는 매니저이며 같은 대상의 30초·1회 뒤 60초·2회 사례와 다른 대상의 방송인 30초 제재 사례를 확인했다.
- `0014`는 `[userId, newNickname, changeType, userFlag, oldNickname]` 순서다. 공식 `setNickname` 메서드와 같은 사용자의 변경 전후 입장 기록을 대조했다.
- `0050`은 `[status, broadcasterId, pollNo, show]` 순서다. 같은 투표 번호에서 `status=1, show=1`은 투표 시작, `status=4, show=1`은 마감과 결과 공개, `status=2, show=0`은 UI 제거와 대조됐다. 원본 숫자와 함께 `pollState` 및 `visible`을 제공하며 질문·선택지·득표수는 채팅 패킷에 없다.
- `0045`의 공식 상품 분기는 일반 퀵뷰 `itemType=1/2/3`을 각각 30/90/365일, 퀵뷰 플러스 `100/101/102/103`을 각각 7/30/90/365일로 표시한다. `itemType=100`은 실방송 화면의 퀵뷰 플러스 7일 이용권 선물과 대조됐으며 `quickViewProduct`와 `durationDays`로 제공한다. 알 수 없는 값은 원본을 보존하고 `unknown`/`null`로 둔다.
- `0001`, `0002`, `0003`, `0009`, `0020`, `0023`, `0033`, `0034`, `0037`, `0038`, `0047`, `0070`, `0071`, `0074`, `0075`, `0076`, `0077`, `0078`, `0092`, `0103`, `0111`, `0126`은 공식 플레이어가 읽는 필드만 추가로 구조화했다. 화면과 대조하지 않은 provenance는 `player`다.
- 기존 `0005`에는 공식 플레이어가 사용하는 BGR 색상, 누적 구독·대표 퍼스널콘 개월, 응원팀 번호를 추가하고 캐리지 리턴을 제거한다. `0091`에는 티어·언어·URL 보정을 추가했으며, `0107 chatNo`는 플레이어가 전달하는 문자열 원본을 유지한다. `0121` 대결미션과 `0141` Nightbot 사유도 공식 분기만큼 구조화한다.
- `LiveView.js`의 내부 구독 상품표 57개 행을 `SubscriptionProduct`로 정규화한다. 공식 UI처럼 `0091`은 `itemType` 또는 `vodItemType`의 첫 일치 행을, `0108`은 `itemType`이 일치하는 선물 행을 사용하며 `0093`에도 첫 일치 상품 정보를 제공한다. 구형 `itemType=1/2/3`은 표 안에서 중복되므로 단일 패킷만으로 유일하게 구분하지 않고 공식 표 순서의 첫 행을 반환한다. 전체 표의 provenance는 `player`이며 화면으로 상품 의미까지 확인된 것은 `0108 itemType=11`의 베이직 1개월 일반 선물권, `0091 itemType=111`의 베이직 1개월 구독, `0091 itemType=103`의 베이직 3개월 정기구독권, `0091 itemType=9200`의 VOD 플러스 구독이다. `9200`은 상품표의 `vodItemType`과 일치하며 `subscriptionSource="vod"`로 제공한다. 일반 상품 번호는 `live`, 상품표에 없으면 `unknown`이며 `live`는 정확한 구매 화면이 아니라 비VOD 상품 번호를 뜻한다. `103` 사례는 기간만 확정하며 화면만으로 선물 플래그까지 확정하지 않는다. 한 사용자의 `0108 itemType=11` 선물 30건 뒤 수신자 중 선물권을 사용한 11명에게 `0091 itemType=111, tier=1`이 이어져 화면과 일치했다. 마지막 캡처에서도 `0108 itemType=11` 수신자와 2.629초 뒤 `0091 itemType=111` 구독자가 일치했다. `subscription_id`·`subscription_nickname`은 구독 대상 방송인과 일치한다. 수신자별 패킷을 임의의 합계 이벤트로 묶지 않는다.
- `0093`의 티어 1은 화면의 “베이직”, 티어 2는 “플러스”와 실방송에서 대조됐다. 원본 숫자 `tier`와 의미값 `subscriptionTier`를 함께 제공한다. 화면 개월은 `month`이며 상품 기간과 다르고, `accumulatedMonth`도 별도 값이다. `itemType=201/203/211/200`의 플러스 25/9/11/4개월째와 `itemType=100/101`의 베이직 9·27/12개월째 문구를 추가로 대조했다. 화면의 커스텀 구독자 명칭은 패킷에 없고 별도 채널 설정에서 오므로 합성하지 않으며, 이 화면은 상품표의 레벨·자동 결제·선물 플래그까지 확정하지 않는다.
- `0109`는 OGQ 이미지 단일 채팅과 OGQ 이미지+텍스트 채팅을 모두 표현한다. 이미지 전용이면 `message`가 비고 텍스트가 함께 보이면 `message`에 담긴다. 플레이어 필드 순서에서 sender id, nickname, flag, BGR color, language, type, extension 및 구독·색상·애니메이션 정보까지 해석한다.
- `0086`은 방송 밖에서 VOD에 후원된 별풍선을 다음 라이브 입장 시 합계로 알린다. 2,894개 화면 표시와 패킷을 대조했으며 방송인·후원자, 합계, 파일명, 기본 효과 여부, chat number, 언어와 URL 보정 값을 구조화한다.
- `0087` 애드벌룬 1개와 양수 `fanOrder`는 화면의 신규 팬클럽 문구와 일치했다. 10,325번째 외에 3,699번째·3,706번째 사례도 대조했다. 공식 `LiveView.js`도 별풍선·애드벌룬·영상풍선에서 `fanOrder > 0`일 때만 이 문구를 만들므로 세 이벤트 모두 `becameFanClub`을 제공한다. `0105 balloonCount=50`, `isDefault=true`는 영상풍선 50개 화면과 대조됐고, `balloonCount=5`, `fanOrder=9725`는 영상풍선 5개와 9,725번째 팬클럽 문구가 함께 표시된 화면과 일치했다.
- `0058` 운영자 공지는 실시간 핫이슈와 별별랭킹 1위 안내에서 관찰됐다. 첫 필드에 본문 전체, 두 번째에 플레이어가 무시하는 `0`, 마지막에 빈 필드가 왔다. 화면의 고정 “SOOP 안내” 제목은 패킷에 없으며 본문만 구조화하고 나머지는 raw에 보존한다.
- `0111`은 같은 드롭스 상품명으로 약 20분 간격으로 6회 수신됐고 `message`와 `imageUrl`은 비어 있었다. 방송에서 드롭스를 안내했고 수신 직후 축하 채팅이 이어졌지만 다시보기에는 해당 안내가 없어 UI 문구나 지급 완료 상태를 추측하지 않는다.
- `0021`은 확장 아이스 모드다. 첫 필드는 얼음 여부, 세 번째 필드는 허용 역할 비트마스크, 네 번째·다섯 번째 필드는 별풍선·구독 제한 수치다. 역할 비트는 방송인 16, 팬클럽 32, 서포터 64, 열혈팬 128, 구독자 256, 매니저 512다. 방송인의 `!얼음` 직후 `0021 [1, ignored, 528, 1, 1]`과 화면 문구를 대조했다. 플레이어가 무시하는 두 번째 필드는 구조화하지 않는다. 함께 수신된 구형 `0019`는 현재 플레이어에 처리 분기가 없어 raw로 유지한다.
- `0121`의 공식 대결미션 type은 `GIFT`/`NOTICE`/`SETTLE`, 도전미션 type은 `CHALLENGE_GIFT`/`CHALLENGE_NOTICE`/`CHALLENGE_SETTLE`다. `missionKey`는 같은 미션의 후원·결과·정산에서 유지된다. 도전미션의 `uuid`, 후원자·방송인, 제목, 후원 개수, 성공·실패 상태, 정산 개수를 구조화하되 전체 JSON을 보존한다.
- 도전미션 수락 대기→스트리머 수락 장면을 포함한 캡처에서 수락 순간의 별도 채팅 WebSocket 패킷은 없었다. `CHALLENGE_GIFT`는 제안·후원 수신이지 수락 완료 상태가 아니므로, 대기·수락·거절 상태를 추측해 만들지 않는다.
- 미션 집중 캡처의 성공 78건은 모두 `SUCCESS → SETTLE → 0125`로 이어졌고 후원 합계와 정산 개수가 일치했다. 실패 7건에는 정산이 없었다. `GIFT` 뒤 결과가 없는 사례는 대기와 거절을 패킷으로 구분할 수 없으므로 추측 상태를 만들지 않는다. 도전미션 후원은 일반 별풍선 `0018`과 중복되지 않았다.
- `0125`의 `list` 항목은 `[userId, nickname, contributionCount, becameFanClubFlag, becameTopFanFlag]`다. `fanOrder`는 fan club 플래그가 1일 때만 화면 순번에 사용된다. 같은 캡처의 100개 정산 두 건에서 기존 팬은 문구가 없고 플래그 1인 참여자만 `fanOrder=7447`과 7,447번째 팬클럽 문구가 일치했으며, 앞선 3684번째·3689번째 사례도 같다. `uuid`는 대응하는 `CHALLENGE_SETTLE`과 같고 `chatNo`와 함께 구조화한다.
- `0013`, `0058`, `0087`, `0107`은 각각 매니저 상태, 운영자 공지, 애드벌룬 효과, 스테이션 애드벌룬의 공식 플레이어 필드 순서를 구조화한다. `0004`, `0008`, `0012`, `0014`, `0021`, `0026`, `0045`, `0050`, `0054`, `0086`, `0090`, `0095`, `0102`, `0118`, `0119`, `0120`, `0127`도 현재 플레이어가 실제로 읽는 필드만 구조화한다. 화면 동작까지 대조하지 않은 이벤트의 provenance는 `player`로 유지하고, `0045`는 화면 대조를 마쳐 `observed`다.
- `0013`과 `0078`의 사용자 플래그는 공식 플레이어의 `flag1`·`flag2`와 `isAdmin`, `isManager`, `isFixedManager`, `isEmployee`, `isEmployeeAdminChat`, `isCleanAti`를 그대로 독립 판정한다. 하나의 임의 역할로 축약하지 않는다. `fixedManager`는 `flag1`의 `64` 비트이며 플레이어에서 `manager`의 추가 기능을 여는 데 사용하지만 별도 화면 명칭은 확인되지 않았다. 새 캡처에서는 한 사용자의 입장 `0004`에 `fixedManager`만 있고 1ms 뒤 `0013`에서 `manager` 비트 `256`이 추가됐으며 퇴장·재입장 뒤에도 같은 순서가 반복됐다. 실방송의 `숲지기26`은 `admin`과 `cleanati`가 동시에 설정되어 여러 권한 비트가 공존함을 확인했으며 `employee`는 아니었다.
- 파서는 알 수 없는 바이트와 opcode를 보존하고, 분할된 헤더·payload 및 WebSocket 메시지 하나에 합쳐진 여러 프로토콜 패킷을 처리해야 한다.
- TLS 인증서 검증을 절대 비활성화하지 않는다. 기존 TypeScript 참고 구현은 검증을 끄지만 이 프로젝트에서는 허용하지 않는다.

## 근거와 신뢰도

- 최우선 근거는 2026-08-16까지 분석한 SOOP 라이브 플레이어 `LivePlayer.js`/`LiveView.js` 빌드 `202603240400`이다. 플레이어 동작과 실방송 관찰 결과가 제3자 라이브러리보다 우선한다. 채팅 enum은 100개이며, 캡처에서만 확인된 `0110`을 더해 known opcode 101개를 유지한다. 같은 번들의 `0115`와 `0500`은 별도 스트리밍 에이전트 프로토콜이므로 채팅 카탈로그에 넣지 않는다.
- TypeScript 참고 구현은 MIT 라이선스의 `reindeer002/soop` 커밋 `ec0ef735bc5aa2b8eaa572c6495a64956f5016f0`이다.
- Java 참고 구현은 MIT 라이선스의 `getCurrentThread/soopapi` 커밋 `fc805ac42051e495c439f88eff3a33cc02eac8c5`다.
- `EVENT_CATALOG.provenance`는 실방송 관찰, 플레이어 코드, 참고 구현만으로 확인한 레이아웃을 구분한다. 참고 구현에서만 확인한 필드 구조를 실방송 검증 완료로 설명하지 않는다.
- 공개 이벤트 타입은 공식 `SVC_*` 상수명을 의미가 드러나는 lower camel case로 바꾸며, 필드명은 플레이어 UI 속성을 역할 중심 camel case로 정규화한다. 플레이어가 무시하고 의미도 확인되지 않은 필드는 추측해서 공개하지 않고 `raw.fields`에 보존한다.
- `0006`, `0017`, `0128`, `0137`은 현재 공식 switch에서도 필드를 읽지 않고 바로 종료한다. `0094`는 처리 분기가 없다. enum에만 있거나 공식 플레이어가 해석하지 않는 이벤트는 공식 이름만 카탈로그에 두고 raw로 유지한다. `0007`은 공식 플레이어가 무시하지만 실방송 관찰 근거로 첫 숫자만 제공한다.

## 데이터와 테스트 fixture

- 실제 사용자 아이디, 닉네임, 메시지, 토큰, 채널 자격 증명이 포함된 캡처 채팅을 커밋하지 않는다.
- 로컬 분석용 `captures.sqlite`, `captures.sqlite-wal`, `captures.sqlite-shm`은 `.gitignore`로 제외하며 npm 패키지에도 포함하지 않는다.
- 테스트에는 합성 패킷 또는 복구할 수 없게 비식별화한 패킷만 사용한다. 선택 실행형 실방송 스모크 테스트는 메모리에서 트래픽을 관찰할 수 있지만 디스크에 기록하면 안 된다.
- raw 패킷 접근은 향후 프로토콜 변경에 대응하기 위한 의도적인 기능이다. 문서에서는 raw 필드에 개인정보가 포함될 수 있음을 사용자에게 알려야 한다.

## 검증 환경 메모

- 2026-08-22 변경은 strict typecheck, 타입 인식 Oxlint, Oxfmt 포맷 검사, `0013`의 `manager+fixedManager` 조합 검증을 포함한 단위 테스트 16개, 최신 캡처 57,857개 전체 재디코딩(실패 0, `fixedManager` 2건 확인)과 `npm pack --dry-run`을 통과했다. 배포 패키지는 문서 포함 37개 파일, 압축 58.2kB다. `npm publish --dry-run`은 레지스트리에 이미 존재하는 `0.1.0` 재게시 금지로 종료되므로 다음 실제 배포 전 버전을 올려야 한다. 이전 변경에서 Chromium·WebKit 스모크를 통과했고, 같은 로컬 환경의 Firefox는 아래에 기록한 Playwright 자체 오류가 재현됐다.
- 같은 로컬 Windows/Node 26.5.1 환경의 Playwright 1.62.1 Firefox 1538은 테스트 본문 전 `browserContext.newPage` 내부의 `_page` 오류로 실패한다. 프로젝트 테스트와 무관한 최소 `firefox.launch() → browser.newPage()`에서도 동일하게 재현됐으므로 라이브러리 실패로 보지 않되, 다른 환경이나 Playwright/Firefox 갱신 뒤 다시 검증한다.

## 향후 기능 관련 메모

- 인증 연결은 `0001`에서 auth ticket을 사용하고, `0002`에서 FTK와 플레이어의 log/auth element를 사용한다. 브라우저 자격 증명 처리는 서버 측 설계가 필요하다.
- 비밀번호 방, 구독플러스 방, 19금 방 접속에는 단순히 다른 패킷을 해석하는 것뿐 아니라 입장 전 entitlement 및 비밀번호 처리 흐름이 필요하다.
- 채팅 전송은 인증된 세션 상태와 opcode `0005`를 사용한다. send API를 추가하기 전에 전송 제한, 운영 제재 오류, 재연결 시의 동작을 조사해야 한다.
